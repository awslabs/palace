Mesh files in this folder originate from the MFEM repository, see
https://github.com/mfem/mfem/tree/master/data.

Mesh files in the `gmsh` subfolder are generated with Julia scripts in such
directory.
