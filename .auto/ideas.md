- Operational note: run roots must stay under /home/ubuntu/workspace, not /tmp; /tmp is on the small root volume and large AMR ParaView output filled it. The measure script now runs Palace from examples/transmon so mesh/transmon.msh2 resolves without a top-level mesh symlink.
Operational storage policy: preserve scalar CSV baselines and an optional origin/main ParaView/GridFunction reference tree at PALACE_AR_REFERENCE_POST_ROOT. Candidate runs must write large output to PALACE_AR_POST_ROOT on /home/ubuntu/workspace and overwrite it each iteration, keeping only the most recent candidate ParaView output. Do not preserve per-commit/per-run ParaView directories. The origin/main reference output files are expected to remain valid because output filenames and values should not change across writer-performance experiments.
- Build harness note: `.auto/measure.sh` now tries a Spack build-env incremental `ninja -C <kept palace-build> -j $PALACE_AR_BUILD_JOBS install` before falling back to `spack install --only package --overwrite --keep-stage`. The fallback intentionally keeps the stage so the next iteration can be incremental.
- Human hypothesis: investigate whether ParaView output can stream directly to file without initializing/materializing a full intermediate variable/buffer. The key question is why GridFunction output is much faster than ParaView when both write similar quantities of data. Compare the GridFunction writer path against the ParaView VTU path: identify whether ParaView pays extra costs for full refined point buffers, XML/base64/compression, per-field/per-value formatting, mesh/geometry duplication, host synchronization, or MFEM high-order/nonconforming traversal. Prefer experiments that isolate writer representation/materialization cost rather than changing solver behavior or dropping output fields.
