# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# SPDX-License-Identifier: Apache-2.0

# This action should be run in/after the palace-ci action.

name: 'Test Palace'
description: 'Run Palace integration tests'
inputs:
  testing-on-build-runner:
    description: 'Set to true if the tests are being run on the machine that built the software (avoids setups)'
    required: false
    default: 'false'
  toolchain:
    description: 'Toolchain to use'
    required: true
  variant:
    description: 'Palace variant'
    required: false
    default: ''
  math-libs:
    description: 'Math libraries'
    required: false
    default: ''
  linear-solver:
    description: 'Linear solver to test'
    required: false
    default: 'Default'
  test-cases:
    description: 'Test cases: "default", "long", or space-separated list'
    required: false
    default: 'default'
  additional-test-cases:
    description: 'Additional test cases to run after test-cases (space-separated list)'
    required: false
    default: ''

runs:
  using: 'composite'
  steps:
    - name: Setup environment
      if: inputs.testing-on-build-runner != 'true'
      uses: ./.github/actions/setup-runner
      with:
        julia-version: 'dont-install'
        spack-version: 'develop'
        llvm-version: ${{ inputs.toolchain == 'llvm' && '19' || 'dont-install' }}
        setup-intel: ${{ inputs.toolchain == 'intel-oneapi' && 'true' || 'false' }}

    # The palace-ci action uploads the spack lock and yaml files. Here, we
    # download them. This ensures we can download the same binaries we built in
    # the previous step.
    - name: Download spack environment
      if: inputs.testing-on-build-runner != 'true'
      uses: actions/download-artifact@v8
      with:
        name: spack-env-${{ runner.arch }}-${{ inputs.toolchain }}-${{ inputs.variant || 'default' }}-${{ inputs.math-libs || 'default' }}

    - name: Install from cache
      if: inputs.testing-on-build-runner != 'true'
      shell: bash
      run: |
        source /tmp/timing_functions.sh
        start_timer
        # --test root builds and installs the palace-unit-tests binary
        # (and its example/ref data). The target is EXCLUDE_FROM_ALL, so
        # without --test root the regression harness binary is never
        # built and `spack load palace` can't expose it. The lock was
        # already concretized with --test root by the build job, so the
        # test deps (catch2) are present in the concrete environment.
        spack -e . install --only-concrete --no-check-signature --test root
        end_timer "Install from cache"

    - name: Run Integration Tests
      shell: bash
      run: |
        source /tmp/timing_functions.sh
        start_timer
        # Use nproc, but halve to account for hyperthreading.
        LOGICAL_CORES=$(nproc)
        THREADS_PER_CORE=$(lscpu | grep "Thread(s) per core" | awk '{print $4}')
        if [ "$THREADS_PER_CORE" = "2" ]; then
            export NUM_PROC_TEST=$((LOGICAL_CORES / 2))
        else
            export NUM_PROC_TEST=$LOGICAL_CORES
        fi

        if [[ "${{ inputs.variant }}" == *"+openmp"* ]]; then
          NUM_PROC_TEST=$(( NUM_PROC_TEST / 2 ))
          export OMP_NUM_THREADS=2
        else
          export OMP_NUM_THREADS=1
        fi

        eval $(spack -e . load --sh palace)

        # Extra Catch2 args baked into every generated ctest command: the
        # linear solver to exercise (varied per job without rebuilding) and,
        # on CUDA builds, the GPU device (which also restricts to np=1).
        LINEAR_SOLVER="${{ inputs.linear-solver }}"
        EXTRA_ARGS="--palace-linear-solver;${LINEAR_SOLVER:-Default}"
        if [[ "${{ inputs.variant }}" == *"+cuda"* ]]; then
          EXTRA_ARGS="${EXTRA_ARGS};--device;cuda"
          export NUM_PROC_TEST=1
        fi

        # Vend the example configs/meshes and reference data from the checkout;
        # the binary reads these at run time (see test/unit/regression_helpers).
        export PALACE_EXAMPLES_DIR="${GITHUB_WORKSPACE}/examples"
        export PALACE_REGRESSION_REF_DIR="${GITHUB_WORKSPACE}/test/examples/ref"

        BIN=$(command -v palace-unit-tests)
        MPIEXEC=$(command -v mpirun)
        GENERATOR="${GITHUB_WORKSPACE}/test/unit/regression/GenerateRegressionCTest.cmake"

        # Generate a relocatable CTestTestfile for a Catch2 filter and drive it
        # with ctest (per-case results, PROCESSORS-correct scheduling). Unlike a
        # build-tree ctest, this works against the Spack-installed binary.
        run_group() {
          local spec="$1" label="$2" timeout="$3"
          local dir
          dir=$(mktemp -d)
          cmake -D PALACE_UNIT_TESTS="$BIN" -D MPIEXEC="$MPIEXEC" \
                -D NP="$NUM_PROC_TEST" -D OUTPUT_DIR="$dir" \
                -D SPEC="$spec" -D LABEL="$label" -D TIMEOUT="$timeout" \
                -D EXTRA_ARGS="$EXTRA_ARGS" -P "$GENERATOR"
          ctest --test-dir "$dir" --output-on-failure
        }

        # Map the action's `test-cases` input onto a Catch2 filter. "default"
        # is the always-on suite; "long" the heavy [Long] cases; anything else
        # is a space-separated list of explicit case names.
        run_cases() {
          local raw="$1"
          if [[ -z "$raw" || "$raw" == "default" ]]; then
            run_group "[Regression]~[Long]" regression 600
          elif [[ "$raw" == "long" ]]; then
            run_group "[Long]" long 1800
          else
            for case in $raw; do
              run_group "$case" long 1800
            done
          fi
        }

        run_cases "${{ inputs.test-cases }}"

        ADDITIONAL="${{ inputs.additional-test-cases }}"
        if [[ -n "$ADDITIONAL" ]]; then
          run_cases "$ADDITIONAL"
        fi

        end_timer "Run Integration Tests"

    - name: Timing information
      shell: bash
      run: |
        cat /tmp/timing.log

