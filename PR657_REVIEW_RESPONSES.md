# PR #657 Review Responses

Tracking file for addressing hughcars' review comments on the `simlapointe/boundarymode-2d` branch.

## Bug Fixes

| #  | File                           | Comment                                                         | Status | Fix                                                                                                                                                    |
|:-- |:------------------------------ |:--------------------------------------------------------------- |:------ |:------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 8  | `boundarymodeoperator.cpp:618` | GMRES restart dim uses `max_it` instead of `max_size`           | FIXED  | Changed `config.linear->max_it` → `config.linear->max_size` in `SetRestartDim()`                                                                       |
| 9  | `boundarymodesolver.cpp:192`   | Non-owning raw pointer in `unique_ptr` — double-free risk       | FIXED  | Non-submesh case now uses caller's `mesh` vector directly via `const auto &fespace_mesh`. No more non-owning unique_ptr or `release()`.                |
| 10 | `densematrix.cpp:49`           | "almost diag" branch drops off-diagonal terms                   | FIXED  | Removed degenerate branch — general 2x2 eigen formula handles `a ≈ b` fine (disc=2\|d\|, v1·v1=2d²>0).                                                 |
| 11 | `materialoperator.cpp:307`     | `eps_imag_scalar` wrong for anisotropic eps*tandelta            | FIXED  | Changed `eps(2,2)*td(2,2)` to `(eps*td)(2,2)` via full matrix multiply, matching `RotateMaterialTensors`.                                              |
| 12 | `lumpedelement.cpp:60`         | 2D branch missing `input_dir` in format string                  | FIXED  | Added `{}` format placeholder and `input_dir` argument to match the 3D warning message.                                                                |
| 13 | `interpolator.cpp:350`         | `by_vdim` indexing wrong — vals always byNODES                  | FIXED  | Removed `by_vdim` check. `InterpolateFunction` is called with `byNODES`, so output is always `byNODES` — always use `vals(d * npts + i)`.              |
| 15 | `geodata.cpp:1643`             | 2D projection skipped when `nodes3d` is null (linear mesh)      | FIXED  | Added `else` branch using `GetVertex()` for linear meshes (same pattern as `ProjectSubmeshTo2D`). Moved `SetCurvature`/node-write outside the if/else. |
| 16 | `mesh.hpp:60`                  | `ceed_from_self` not reset in `Update()` — stale flag after AMR | FIXED  | Added `ceed_from_self = false;` at the top of `Mesh::Update()`.                                                                                        |

## Code Quality

| #  | File                       | Comment                                                                | Status    | Fix                                                                                                                                                                                                            |
|:-- |:-------------------------- |:---------------------------------------------------------------------- |:--------- |:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 14 | `postoperatorcsv.cpp:983`  | Copy-paste duplicate block (dead code)                                 | NOT FOUND | Could not reproduce in current code — may have been fixed in a prior commit.                                                                                                                                   |
| 17 | `postoperator.cpp:1853`    | V_postpro is nondimensional — needs dimensionalization                 | FIXED     | Added `Dimensionalize<VOLTAGE>(1.0)` scaling in `Measurement::Dimensionalize` for `mode_data.V`.                                                                                                               |
| 19 | `postoperatorcsv.cpp:1056` | `abs(V) > 0.0` conflates no-coords with zero voltage                   | FIXED     | Changed to check `table.has(key)` (column existence) instead of checking voltage magnitude. Added `Table::has()` method.                                                                                       |
| 20 | `iodata.cpp:435`           | Missing BOUNDARYMODE config validation                                 | FIXED     | Added BOUNDARYMODE case in `CheckConfiguration`: verifies `n >= 1`, warns on incompatible BCs (lumped ports, surface currents).                                                                                |
| 37 | `configfile.hpp:616`       | Dead field `index` — never parsed or read                              | FIXED     | Removed `ModeVoltageData::index`.                                                                                                                                                                              |
| 38 | `configfile.cpp:919`       | `ModeVoltageData.voltage_attributes` not added to attributes aggregate | FIXED     | Added loop to insert `voltage` map attributes into the postpro attributes aggregate.                                                                                                                           |
| 39 | `postoperator.cpp:213`     | Only first impedance/voltage entry used, rest silently ignored         | FIXED     | Full multi-entry support: PostOperator stores per-index config maps, ModeData uses `map<int, ModeImpedanceResult>` and `map<int, ModeVoltageResult>`, CSV columns are per-index (e.g., `Z_PV[1]`, `\|V[2]\|`). |

## Docs / Style

| #  | File                            | Comment                                                         | Status | Fix                                                                                                                                                                  |
|:-- |:------------------------------- |:--------------------------------------------------------------- |:------ |:-------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | `boundaries.md:381`             | IntegrationOrder confusing — maps to quadrature order in solve? | FIXED  | Renamed `IntegrationOrder` → `NSamples` throughout (config, schema, docs, code). Clarified it's uniform sample count with GSLIB interpolation, not Gauss quadrature. |
| 2  | `boundaries.md:430`             | Is this 100-sample uniform or quadrature?                       | FIXED  | Same as above.                                                                                                                                                       |
| 3  | `boundaries.md:725`             | Add mutual exclusion clause for CurrentAttributes               | FIXED  | Added clause: "Either CurrentAttributes or CurrentPath may be specified."                                                                                            |
| 4  | `boundaries.md:730`             | Clarify VoltagePath uses uniform weighting                      | FIXED  | Added note: uses uniformly weighted GSLIB interpolation, less accurate than attribute-based, doesn't require mesh modification.                                      |
| 5  | `cpw2d.md:12`                   | Fix text, add link to solvers                                   | FIXED  | Changed to `"BoundaryMode"` with link to solver docs.                                                                                                                |
| 6  | `cpw2d.md:30`                   | Maybe link                                                      | FIXED  | Added links to problem type and solver config docs.                                                                                                                  |
| 7  | `cpw2d.md:48`                   | Closest or only above?                                          | FIXED  | Clarified shift-and-invert finds modes closest to (not necessarily above) target.                                                                                    |
| 9  | `cpw2d.md:72`                   | NSamples not IntegrationOrder                                   | FIXED  | Renamed throughout (see #1).                                                                                                                                         |
| 10 | `problem.md:180`                | Long line                                                       | FIXED  | Wrapped.                                                                                                                                                             |
| 11 | `cpw2d.md:1`                    | Maybe add images                                                | NOTED  | Agreed — nice to show off. Images can be added in a follow-up.                                                                                                       |
| 12 | `cavity2d.json:50`              | 2D eigenmode vs 2D boundary mode?                               | NOTED  | This is a 2D eigenmode — cavity2d demonstrates that all solver types work with 2D meshes, not just BoundaryMode.                                                     |
| 13 | `cavity2d_driven.json:54`       | Indenting is off                                                | FIXED  | Corrected indentation.                                                                                                                                               |
| 14 | `cavity2d_magnetostatic.json:1` | 2D for all solver types?                                        | NOTED  | Yes — 2D meshes are supported for all solver types. Magnetostatics uses the in-plane A-field (TM mode, scalar A). Documented in the guide.                           |
| 15 | `cpw_wave_uniform.json:76`      | Debris                                                          | FIXED  | Removed trailing `Verbose: 0` (default).                                                                                                                             |
| 16 | `mesh.jl:5`                     | Style consistency?                                              | NOTED  | The mesh.jl files use DeviceLayout.jl Gmsh API, consistent with other examples.                                                                                      |
| 17 | `mesh_thick.jl:26`              | Strange indenting on trailing comments                          | FIXED  | Aligned comment indentation consistently.                                                                                                                            |
| 18 | `solver.md:452`                 | MaxSize default `[0]` vs code `-1`                              | NOTED  | `[0]` is the docs convention for "use default" across all solver types (Eigenmode uses the same pattern). Code internally maps 0 → -1 → default.                     |
| 19 | `cpw2d.md:39`                   | Target 2.5 vs 2.497                                             | FIXED  | Changed snippet to `2.497` to match `cpw2d_thin.json`.                                                                                                               |
| 20 | `solver.md:553`                 | Broken sentence "coupled Nedelec - H1 system"                   | FIXED  | Fixed to "coupled Nedelec–H1 system." (single line, en-dash).                                                                                                        |
| 21 | `runtests.jl:617`               | 2e-2 tolerance is lax                                           | NOTED  | Added comment explaining: coarse 2D meshes with low-order ports are sensitive to MPI partitioning.                                                                   |
| 22 | `runtests.jl:691`               | 1e-2 also lax                                                   | NOTED  | Added comment: impedance depends on voltage path integration, tolerant for cross-platform reproducibility.                                                           |
| 23 | `solver.json:139`               | MaxSize schema forbids 0                                        | FIXED  | Changed `exclusiveMinimum: 0` → `minimum: 0` to allow 0 (means "use default").                                                                                       |

## Architecture

(Requires discussion — not started. See hughcars' top-level review comment about `BoundaryModeSolver → BoundaryModeOperator → WavePortOperator` reorganization.)
